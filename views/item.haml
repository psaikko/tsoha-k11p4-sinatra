%p
  Name: 
  #{@item.name}
%p Description
%p #{@item.text}
%p 
  Sold by:
  #{@item.user.name}
%p
%table{:border => "1"}
  %tr
    %th{:colspan => "3"} Bids on this item
  %tr
    %th Amount
    %th Bidder
    %th Time
  - @item.bids.all(:order => [:amount.desc]).each do |bid|
    %tr
      %td #{bid.amount}
      %td #{bid.user.name}
      %td #{bid.made_at}
%p 
  Current price: 
  #{@item.current_price}
- if @name != nil
  %p Make a bid:
  %form{:method => "post", :action => "/bid/" + @item.item_id.to_s}
    %input{:type => "text", :width => "10", :name => "amount"}
    %input{:type => "submit", :value => "Bid!"}
  

